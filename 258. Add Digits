class Solution {
    public int addDigits(int num) {
        
        if(num<0){
            return 0;
        }
        else{
             while(num>9){
                num=(num/10)+(num%10);
        }
        return num;
        }
       
    }
}
